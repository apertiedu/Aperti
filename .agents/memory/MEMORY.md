- [Aperti AI config](aperti-ai-config.md) — NVIDIA_API_KEY drives AI; ai.ts is canonical; ai-gateway.ts priority must be NVIDIA → Replit → OpenAI; AI_CONFIG already exported at bottom of ai.ts (do not add a second one).
- [Aperti auth pattern](aperti-auth-pattern.md) — JWT via httpOnly cookies (credentials:"include"); token is never exposed to JS; test-runner token guards must be removed so cookies authenticate instead.
- [Aperti route paths](aperti-route-paths.md) — All frontend fetch() calls need /api/ prefix; dashboard sub-routes mount at /api/dashboard/; missing prefix causes silent 404s with null data (no crash because fallbacks handle it).
- [Phase 34 implementation](phase34.md) — session slots, QR center, data quality, route health, feature status all implemented and live.
- [Sidebar logo pattern](sidebar-logo.md) — logo section uses text-only "Aperti." — no icon. The "Educational OS" subtitle was removed in Phase 34 to simplify the logo.
- [DB migration pattern](db-migration.md) — use `psql "$DATABASE_URL" -c "..."` for migrations; node -e with `pg` doesn't work from workspace root (no pg module there).
- [Session slots schema](session-slots-schema.md) — new `session_slots` table added, exported from lib/db/src/schema/index.ts, FK to lessons(id).
- [Aperti Phase 35 features](aperti-phase35.md) — DAU/WAU/MAU endpoint, command palette navigation + TYPE_ICONS, maxLength on all password inputs, focus-visible CSS, CORS ALLOWED_ORIGINS, extractErrorMessage in api.ts
- [Aperti Phase 36 features](aperti-phase36.md) — Emergency tools, platform health score, recently visited, 404 page, CSS hero, core-hub smart context, admin health bars, CSS micro-interactions
- [Aperti Phase 37 TypeScript fixes](aperti-phase37.md) — queryFn pattern: apiFetch returns Response; must add .then(r=>r.json()) and type useQuery<T> explicitly or TypeScript errors cascade. Icon types need style prop included.
- [Aperti Phase 39 motion system](aperti-phase39.md) — shared motion lib at src/lib/motion.ts; useCountUp hook; attention-pulse CSS class; Progress component uses 600ms cubic-bezier transition; data-quality has Repair All Fixable button.
- [Aperti B4-B12 stabilization](aperti-b4-b12.md) — route registry, ApiError class, logError util, test runner, repair-all endpoint, marquee strip all implemented.
- [Aperti Phase 42 components](aperti-phase42.md) — AppButton, AppCard, AppModal, AppTable, AppErrorState, feature-registry.ts, FeatureRegistryPage all created; breadcrumb enhanced; skeleton variants added; CSS design polish applied.
- [Aperti Phase 43 stability](aperti-phase43.md) — AIErrorBoundary class component, enrollment audit page/route, instapay duplicate check, device manager revoke, ServerError route in all routers, live-counts + error monitor in admin command, engagement/risk/consistency analytics, honest landing stats.
- [Aperti Phase 44 zero-friction](aperti-phase44.md) — SaveIndicator, course archive, Platform Health, FAQ fallback, notification grouping, TutorCraft timeout, slot conflict detection, AI reliability pass (assessment-builder + grade-flow), mobile safe-area CSS, touch targets audit all complete.
- [Aperti Phase 46 production-lock](aperti-phase46-production-lock.md) — 95/100 score, Vite manual chunks, TanStack gcTime, 1yr static asset cache, simulation script, README rollback, 8 deliverable docs.
- [Aperti Phase 48 final stack](aperti-phase48.md) — SSE AI gateway, multi-agent orchestrator (teacher/student/admin), V2 namespace, classforge mount, ai_shared_memory table, live SSE debug dashboard all complete.
- [Aperti Phase 47 V2 Upgrade](aperti-phase47.md) — permission matrix, AI Teaching Assistant (4 modules), repair script, deploy pipeline, launch cert — all in existing structure (no aperti-v2/ restructure).
- [Aperti credentials recovery](aperti-credentials-recovery.md) — root cause of ~80% of 401/undefined errors: local apiFetch/fetchJSON without credentials:"include". 18 files fixed. Pattern to check: any file with a local fetch wrapper must have credentials:"include".
- [Aperti admin-repair table names](aperti-admin-repair-tables.md) — orphans/fix-orphans endpoints must use course_enrollments (not enrollments), session_id (not lesson_id) on attendance, guardian_links_no_student (not expired_sessions). Wrong table names cause -1 count errors silently.
- [anime.js v4 API](animejs-v4-api.md) — v4 has no default export; use named `animate(target, params)` + `stagger()`, ease strings drop `ease` prefix (e.g. `outExpo` not `easeOutExpo`), callbacks are `onUpdate`/`onComplete`.
- [Landing page dark overhaul](landing-dark-overhaul.md) — dark cinematic design: hero #060D1B, stats strip dark, CTA dark, footer 4-col dark. EarlyAccessForm takes `dark?:boolean` prop. InteractiveDemo is defined but must be explicitly mounted. GetStartedSteps uses icon components not emoji strings. Mobile: scroll-snap disabled at 640px.
- [Parent dashboard SQL pitfalls](parent-dashboard-sql.md) — exams has no student_id; use student_marks for student filtering. Column is exam_date not date. ascend_profiles uses student_account_id. sessions table does not exist. student_marks uses marked_at not graded_at. guardian_links must be status='active' (not 'approved'). parentDashboardRouter must be explicitly mounted at /parent in index.ts.
- [Dark-mode sweep](dark-mode-sweep.md) — bg-white → bg-card, border-gray-100 → border-border, text-gray-900 → text-foreground sweep complete across all authenticated flows; trust/landing/login left white by design; bg-white/* transparency overlays are intentional.
- [Aperti Phase 49 premium UI](aperti-phase49.md) — aurora-bg, float-orb-*, section-badge, card-premium, card-tilt-glow, shimmer-sweep, blur-reveal, border-pulse CSS classes all added; Reveal component uses blur(6px) filter; Feature3DTiltCard uses ICON_THEMES array; nav uses nav-link-premium underline; layout page transitions use blur(3px); motion value .get() must NOT be used in JSX (not reactive — use static radial or useTransform instead).
- [Aperti Phase 50 production overhaul](aperti-phase50.md) — registerLimiter on /auth/register + /student-register; subscribeLimiter on commerce subscribe (no custom keyGenerator!); sanitizeBody middleware; graceful SIGTERM shutdown; subscribe.tsx now uses @/lib/api (had missing credentials:include); RouteProgressBar + NetworkStatusBanner components; mobile CSS safety net.
- [Aperti try-catch hardening](aperti-trycatch-hardening.md) — 21 backend routes fully wrapped (ascend, coupons, exam, exams, gradebook, grading, goals, helpdesk, notebooks, payments, resources, recordings, centers, change-password, sessions, notifications, focus-coach, online-exams, echo-profile, admin-settings, scan-scribe). All use res.status(500).json({error: "..."}) — never throw unhandled. NVIDIA model=openai/gpt-oss-20b set in index.ts via process.env.OPENAI_MODEL.
- [Replit migration decisions](replit-migration.md) — JWT_SECRET + DATABASE_URL configured as Replit secrets; custom JWT auth retained (no Replit Auth swap needed); Backend API on port 3001, Vite frontend on port 5000; per-router ErrorBoundary wrapping added to StudentRouter/TeackerRouter/AdminRouter/ParentRouter; QueryClient uses smart retry (skips 401/403/404) + networkMode offlineFirst; api.ts adds 20s AbortController timeout to all requests; useAnimeCounter integrated in study-stream stat cards.
- [Aperti auth null-token pattern](aperti-auth-null-token.md) — useAuth() always returns token:null (cookie-only auth); any page sending Bearer ${token} sends "Bearer null" which server ignores; must use credentials:"include" instead. Also: register.tsx previously stored JWT in localStorage — always check new registration paths for localStorage.setItem("aperti_token").
- [Aperti CMS tables](aperti-cms-tables.md) — testimonials, faqs, landing_sections tables must be created via psql migration (not in Drizzle schema). subscription_plans has no badge/is_highlighted columns — use CASE on sort_order in query. /api/waitlist/join posts to waitlist_submissions table. launch-cms.ts mounts all landing + feature routes.
- [Aperti Phase 16 Micro-UX](aperti-phase16.md) — 7 components (page-transition, empty-state, status-button, form-field, dashboard-greeting, trust-signals, loading-state) + barrel at phase16.ts; ~280 lines CSS; applied to 10 pages. useMutationStatus is a plain function safe to call inline in JSX.
- [Content Calendar](content-calendar.md) — admin-only /admin/content-calendar; table created via raw psql (NOT in Drizzle schema); 7 REST endpoints; per-type payload fields; mini calendar with dot indicators.
- [Zero-Defect Audit patterns](zero-defect-audit.md) — TS fixes: cast motion.div spread to any, add guest key to roleBadgeColor, useRef<T|undefined>(undefined), placeholderData no type annotation, unknown payload && → !! prefix, ElementType → ComponentType<{size?:number}>. lucide-react + date-fns must be vendor chunks. alert()/prompt() → toast everywhere; window.confirm() ok for admin delete guards only. useDraftRecovery confirm() → toast with React.createElement(ToastAction,...) as any.
- [System diagnostics pattern](system-diagnostics.md) — /api/system/* routes in system.ts use requireRole("admin","super_admin"); OpenAI ping uses AbortController 8s timeout + direct fetch to BASE_URL/chat/completions; never expose key values to frontend (only boolean presence); /admin/system-diagnostics page at App.tsx + layout.tsx nav entry under "System Diagnostics".
- [Aperti AI reliability overhaul](aperti-ai-reliability-overhaul.md) — ai_interactions has no `success` BOOLEAN; use `status='error'` TEXT; SnapGrade human-override pattern; trackFailure must be in every gateway catch block.
- [Aperti AI Governance v2](aperti-ai-governance-v2.md) — 3-tier confidence (≥0.85=high/≥0.65=medium/<0.65=low), ai_grade_reviews/ai_learning_events/teacher_ai_stats tables created via psql; governance router at /api/ai-governance; rubric generator returns suggested-only rubrics.
- [Aperti production hardening](aperti-production-hardening.md) — JWT hardening + full observability layer: safeHandler, requestObserver, AI validator, safe mode, self-check, /admin/production-hardening dashboard.
- [Aperti Phase 51 AI Intelligence Suite](aperti-phase51.md) — Personal Tutor (/api/ai-tutor), Smart Study Plan (/api/study-plan), Grade Prediction (/api/grade-prediction); all mounted in app.ts; student routes + nav entries added; graceful fallback when no AI key; study plan enforces exam_date hard constraint.
- [Aperti production hardening pass](aperti-production-hardening-pass.md) — All Phase 51 routes wrapped with safeHandler + validateAIResponse; safe-handler AI detection expanded; /api/errors/ux-violation endpoint live; /api/system/validation-errors endpoint added; System Health Dashboard at /admin/os/health replaces old HealthPage; useUxGuard hook created.
