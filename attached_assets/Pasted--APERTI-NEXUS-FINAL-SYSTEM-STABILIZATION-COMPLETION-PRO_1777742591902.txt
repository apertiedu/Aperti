🚀 APERTI NEXUS — FINAL SYSTEM STABILIZATION & COMPLETION PROMPT (PRODUCTION READY)

This is the FINAL BUILD + FIX + STABILIZATION PROMPT for the entire Aperti Nexus system.

The goal is no longer feature expansion — it is:

«🧠 Make EVERYTHING work correctly, consistently, securely, and smoothly in production.»

---

🛠️ GLOBAL SYSTEM REQUIREMENT

Before finalizing:

- Fix ALL broken routes
- Fix ALL blank pages
- Fix ALL login/session issues
- Fix ALL API crashes
- Fix ALL missing dependencies
- Fix ALL undefined states
- Ensure database integrity
- Ensure role-based access works everywhere

NO PARTIAL FEATURES ALLOWED IN FINAL BUILD.

---

🔐 AUTH SYSTEM FINAL FIX

Ensure:

- login works for ALL roles:
  
  - student
  - teacher
  - assistant
  - parent
  - admin

- session persistence is stable

- cookies work correctly (secure + httpOnly)

- no logout bugs after navigation

- no blank dashboard after login

---

👤 STUDENT ONBOARDING FINAL FIX

Ensure:

- student self-registration works ONLY if:
  
  - code exists
  - name matches uppercase record
  - teacher exists

- account creation:
  
  - does NOT duplicate users
  - correctly links teacherId
  - initializes student profile properly

---

🌐 ROUTING & PAGE FIXES

Ensure:

- no blank pages anywhere
- all routes protected properly
- role-based redirects work:
  - /login → role selection
  - /dashboard → correct role dashboard
- guest page loads correctly without auth

---

🧠 AI SYSTEM STABILITY CHECK

Ensure:

- AI tutor does NOT crash if no data exists
- AI marking engine handles empty submissions safely
- AI report generator never returns null
- fallback responses exist everywhere

---

📊 DATABASE INTEGRITY FIX

Ensure:

- no orphan records
- correct foreign key linking:
  - students → teachers
  - parents → students
  - exams → teacherId
  - attendance → sessionId
- no duplicate student codes per teacher
- safe deletion does not break relationships

---

🏠 HOMEWORK SYSTEM FIX

Ensure:

- submission works for all file types
- AI marking does not fail on missing MS
- teacher review override works
- attachments upload correctly
- deadlines enforced properly

---

🧪 EXAM SYSTEM FIX

Ensure:

- exam creation does not crash on empty question tree
- marking scheme parsing works
- results calculation always returns valid percentage
- grade curve system always returns valid grade
- deleted exams remove all dependent data safely

---

📚 QUESTION BANK FIX

Ensure:

- past paper uploads do not break parsing
- questions always link to topics
- duplicate questions are handled safely
- AI classification does not fail on malformed PDFs

---

🌐 PUBLIC PAGE FIX

Ensure:

- guest landing page loads fast
- teacher/course/center data displays correctly
- trial booking never fails silently
- pricing always renders correctly
- no undefined course/teacher crashes

---

👨‍👩‍👧 PARENT SYSTEM FIX

Ensure:

- parent dashboards never show empty crashes
- child linking is always valid
- AI explanations always fallback to safe text
- alerts system does not spam or duplicate

---

🔁 SYSTEM MODE SWITCH FIX

Ensure:

- FULL MODE and WHATSAPP MODE switching works instantly
- no mixed state between modes
- UI adapts correctly per mode
- database logic respects mode selection

---

🔐 SECURITY FINAL AUDIT

Ensure:

- strict role enforcement everywhere
- no unauthorized API access
- admin override logs recorded
- no data leaks across teachers
- password hashing verified
- session hijacking protection active

---

📊 REPORT SYSTEM FINAL FIX

Ensure:

- CSV export ALWAYS returns single-column format
- reports never break formatting
- AI reports never return empty strings
- all emojis and formatting preserved
- bulk export does not crash system

---

🎨 UI FINAL FIX

Ensure:

- no broken layouts
- no missing components
- no undefined states in UI
- smooth navigation transitions
- loading states everywhere
- mobile responsiveness fully working

---

⚡ PERFORMANCE FINAL OPTIMIZATION

Ensure:

- no slow dashboard loads
- lazy loading implemented
- caching enabled for analytics
- bulk operations do not freeze system
- large PDFs handled without crash

---

🧠 FINAL AI SAFETY LAYER

Add fallback rules:

If AI fails:

- return safe explanation message
- never return null
- never crash UI
- always degrade gracefully

---

🚀 FINAL ACCEPTANCE CRITERIA (SYSTEM IS COMPLETE WHEN):

✔ No blank pages anywhere
✔ No login/session bugs
✔ No broken APIs
✔ No role leakage
✔ No crashes on AI features
✔ All dashboards load correctly
✔ Public page fully functional
✔ Exam + homework + attendance fully stable
✔ Reports export correctly
✔ Admin controls fully working
✔ System runs end-to-end without errors

---

🧠 FINAL OUTPUT GOAL

Aperti Nexus must now behave as:

«A fully stable, production-grade AI education operating system with no missing functionality, no broken flows, and no partial implementations.»

---

🚀 END STATE

Once this is applied:

The system should be:

- stable
- complete
- production-ready
- deployable immediately
- fully integrated across all modules

NO FEATURE ADDITION AFTER THIS POINT — ONLY STABILITY AND FIXES.